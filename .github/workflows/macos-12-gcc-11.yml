name: macos-12-gcc -std=11
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
jobs:
  build:
    runs-on: macos-12
    steps:
    - uses: actions/checkout@v3
    - name: make check
      run: make check CXX=g++ CXXFLAGS=-std=11
