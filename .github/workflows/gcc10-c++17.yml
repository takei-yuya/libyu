name: gcc-10 c++17
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: gcc:10
    steps:
    - uses: actions/checkout@v4
    - name: make check
      run: make check CXXFLAGS=-std=c++17
