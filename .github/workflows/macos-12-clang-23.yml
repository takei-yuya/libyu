name: macos-12-clang -std=23
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
jobs:
  build:
    runs-on: macos-12
    steps:
    - uses: actions/checkout@v3
    - name: make check
      run: make check CXX=clang++ -std=23
